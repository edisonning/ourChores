# 家务天平 v2 素材来源

用户提供于 2026-09-11，原始包 `output/imagegen/scale-v2-assets.zip`。本目录保留原包四张透明 PNG，未重绘或处理像素。原生成提示词见 `prompt.txt`，转轴及裁切坐标见 `shared/scale-geometry.json`。

网页组件通过导入使用同一份图片，小程序通过静态资源加载。横梁绕原转轴旋转，两侧托盘以吊点反向旋转抵消倾斜；支架固定，动画持续 650ms。
